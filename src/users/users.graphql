scalar Date

type Query {
    getUsers: [User]
}

type User {
    id: ID!
    name: String
    email: String
    passwordHash: String
    passwordSalt: String
    createdAt: String
    updatedAt: String
    deletedAt: String
    role: Role
}