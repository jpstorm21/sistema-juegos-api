scalar Date

type Query {
    getUsers: [User]
}

type User {
    id: ID!
    name: String
    email: String
    passwordHash: String
    passwordSalt: String
    createdAt: Date
    updatedAt: Date
    deletedAt: Date
    role: Role
}